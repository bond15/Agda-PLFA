-- use literate Agda
-- https://agda.readthedocs.io/en/v2.6.1/tools/literate-programming.html



-- note equality vs isomorphism

-- the analogy
-- + ≃ ⨄  interpret OR
-- * ≃ x  interpret AND
-- ^ ≃ →  interpret IMPLIES


-- oof
-- https://en.wikipedia.org/wiki/Hyperoperation
-- https://github.com/mokus0/junkbox/blob/master/Agda/HyperOperation.agda

-- cardinality of finite sets
-- where are subtraction and division?
-- only "positive fragment?"


-- * associativity
-- n * (m * p) = (n * m ) * p

-- * comm
-- n * m = m * n


-- + assoc

-- + comm
-- n + m = m + n


-- exponent laws
-- aⁿ x aᵐ = a⁽ⁿ⁺ᵐ⁾
-- a ^ n ^ m = a ^ (n * m)
-- a ^ 0 = 1

-- * + distribute
-- (a + b) * c = (a * c) + (b x c)

-- + * distribute  (embedding not isomorphic)
-- (a * b) + c ≲ (a + c) * (b + c)




-- * ^ distribute?







-- 0 * a = 0
-- a * 0 = 0

-- 1 * a = a
-- a * 1 = a

-- 0 + a = a
-- a + 0 = a

-- a
